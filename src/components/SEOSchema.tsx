import { Helmet } from "react-helmet-async";

const SEOSchema = () => {
  return (
    <Helmet>
      {/* Note: JSON-LD schemas are now in index.html for bot visibility */}
      {/* This component only handles dynamic meta tags */}
    </Helmet>
  );
};

export default SEOSchema;
